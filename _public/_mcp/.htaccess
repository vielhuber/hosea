# forward Authorization header

RewriteEngine on
RewriteBase /
CGIPassAuth On
RewriteCond %{HTTP:Authorization} ^(._)
RewriteRule ._ - [e=HTTP_AUTHORIZATION:%1]

# block public access to configuration and logs

<FilesMatch "(^\.env$|\.log$)">
Require all denied
</FilesMatch>
